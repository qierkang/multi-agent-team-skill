# 任务包：TASK-000

## 目标

- 明确、可验证的一句话结果。

## 背景与入口

- 必读文件：
- 已知事实：
- 非目标：

## 文件边界

- 允许读取：
- 允许写入：
- 禁止修改：
- 文件所有权：
- 项目根目录：必须是 inspect 已确认的根目录；禁止写入另一 checkout/worktree 或根目录之外。

## 验证

- 验证命令：
- 验收标准：
- 需要留存的证据路径：

## 停止与升级条件

- 连续两次同因失败时停止并提交现场。
- 发现路径冲突、跨 checkout/worktree、权限不足、需求矛盾或高风险外部动作时暂停。

## 回传格式

1. 状态：
2. 结论：
3. 修改范围：
4. 验证结果：
5. 风险：
6. 证据路径：
