# 最小派发包：TASK-000

> 仅用于低风险、单目标、短时、可机械验证的 fast lane 任务。

- 目标：
- 允许写入：
- 项目根目录：仅允许 inspect 已确认根目录内的相对路径，禁止另一 checkout/worktree。
- 禁止修改：
- 验证命令：
- 完成证据：
- 停止条件：失败一次、出现范围歧义、路径冲突或外部高风险动作即停止并转完整任务包。
- 审查策略：默认 `on-failure`；风险升高后必须改用全新 reviewer。
